export default defineNuxtPlugin(() => {
    const router = useRouter();
    // Log all the routes
    //console.log(router.getRoutes());

    // You can also do something with the routes here if needed
});